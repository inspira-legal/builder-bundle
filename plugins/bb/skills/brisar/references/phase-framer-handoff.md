# Phase Framer-handoff: bypass scaffold, generate handoff to HARPA

Replaces Phase 2 (gate) + Phase 3 (scaffold) + Phase 4 (design-direction) **only when** `brand.workflow == framer-harpa`. The builder is building on the Site Institucional, which lives in Framer + `harpa-lpbuilder`, not in Vite + React.

## Why this phase exists

The standard brisar flow (Vite + Tailwind in `prototype/`) is incompatible with Framer:

- Framer doesn't accept Tailwind nor CSS modules, code components use inline styles only.
- Tokens live as Framer color styles (`/Rich Black`, `/Centaurea`, `/Cornflower`), not as CSS vars.
- The `harpa-lpbuilder` bundle already has rules equivalent to tokens.md/components.md (in `.claude/rules/`), plus the `mcp-unframer-co` MCP to sync with live Framer.

The wrong thing would be to scaffold a React app here. The right thing is **to capture the intent from the lightning intake and deliver it as context for the builder to use inside harpa-lpbuilder**.

## What this phase does

Just one thing: writes `harpa-handoff-<slug>-<YYYY-MM-DD>.md` in the cwd with what the builder will paste (or reference) in the Claude session inside `harpa-lpbuilder/`. Doesn't create a folder, doesn't scaffold, doesn't touch Framer files.

## Step 1: check if the harpa-lpbuilder repo exists locally

```bash
test -d ~/Desktop/harpa-lpbuilder && echo "found-desktop" \
  || test -d ./harpa-lpbuilder && echo "found-cwd" \
  || find ~ -maxdepth 4 -type d -name "harpa-lpbuilder" -not -path "*/node_modules/*" 2>/dev/null | head -3
```

Three outcomes:

- **Found**: record the path. Will cite it in the terminal handoff.
- **Not found**: record `harpa_repo_path: null`. The handoff will include the `git clone` in Step 5.

## Step 2: Write harpa-handoff-<slug>-<date>.md

Use Write to create the file in the cwd. Template:

```markdown
# HARPA handoff: <slug>

> Generated by /bb:brisar on <date>.
> Use this file inside `harpa-lpbuilder/` when you open Claude Code there.

## Quick context

- **Slug:** <slug>
- **Framer scope:** <new-page | section-in-existing | edit-existing | exploration>
- **Appetite:** <appetite>
- **Brand:** Institutional site (Inspira)
- **Stack:** Framer canvas + React/TS code components with inline styles only

## What I am building (original intent)

<intent.raw_prompt from Phase 1>

## How to open the session (paste this into the first message of Claude Code inside harpa-lpbuilder)
```

I want <intent.raw_prompt>.

Scope: <framer.scope>.
Appetite: <appetite>.

Before doing anything:

1. Run `mcp__mcp-unframer-co__getProjectXml` to confirm Harpa is in focus in the Framer desktop app. If the wrong project comes back (Mode B), tell me before going on.
2. Read `.claude/rules/design-tokens.md` and `.claude/rules/components.md` (Mode A. Live source of truth; Mode C, fallback in `references/`).
3. For `<framer.scope == new-page>`: scan the Building Blocks page via `getNodeXml` and propose 2-3 compositions to me before creating the page.
   For `<framer.scope == section-in-existing>`: ask me which page is the target, then `getNodeXml` of that page, and propose where the section goes.
   For `<framer.scope == edit-existing>`: ask me which element is the target (route + block name), then `getNodeXml` of that specific node.
   For `<framer.scope == exploration>`: start in Mode C (code-only draft), I will paste it into Framer by hand later.
4. Reuse first: never create a new component without confirming with me that no existing one serves.
5. Responsive first: desktop → tablet (810px) → mobile (390px) overrides.
6. Inline styles only. WCAG AA.

```

## Visual direction (draft: refine it in the session)

<If framer.scope ∈ {new-page, section-in-existing}, write 3-5 bullets of visual direction in Framer idiom:>

- **Composition:** [how to organize Stacks/Frames, e.g. "full-bleed hero with a centered vertical Stack: Eyebrow + H1 (Instrument Serif for a hero, Poppins Semibold otherwise) + sub + primary CTA"]
- **Components to reuse:** [list the blocks that exist in Building Blocks, e.g. "Hero/Centered, CTA/Primary, Section/SplitWithImage"]
- **Key tokens:** [`/Rich Black 100%` for headlines, `/Cornflower 100%` for the CTA, `/Cool Gray 60%` for body, always commented in the code components with the Framer style name]
- **Responsive notes:** ["on mobile the split becomes a vertical stack; full-width CTA below 600px"]
- **States:** [where it applies: CTA hover, input focus, empty state of a CMS list, and so on]

<If framer.scope == edit-existing: skip this section. Fine tuning is better decided in the session with the XML at hand.>

## Open questions (decide them in the Framer session, not here)

- Content (copy, images): who supplies it, and when.
- CMS: reuse an existing collection, or create one?
- Animation: Framer Motion variants, or hover states only?
- Metric: track conversion? Where?

## Risks / heads-up

<Include what is relevant:>
- If `framer.scope == edit-existing` AND `appetite == 1 day`: warn that live changes need Mode A (live MCP), not Mode C, or two divergent paths come out of it.
- If `framer.scope == new-page` AND `appetite == 1 day`: producing a full page in hours is viable only by reusing blocks. This is not the moment for a new component.
- If `framer.scope == exploration`: stay in Mode C; Framer only receives the result after local visual validation.

---

Next step: see the /bb:brisar terminal (Framer variant in phase-5-handoff.md).
```

## Step 3: close the journey in the brief

This path does NOT scaffold, so the handoff file stays in the cwd where `/bb:brisar` was run.
The brief stays where Phase 1 opened it, `.bb/<slug>/design.md`. Set its frontmatter
`status: completed`, `phase: done`, and record in prose:

- the brand (`site-institucional`, workflow `framer-harpa`, from the external bundle), the
  fidelity (`framer-canvas`, hosted in Framer via harpa) and the appetite
- the scope (`<framer.scope>`), the name of the handoff file
  (`harpa-handoff-<slug>-<YYYY-MM-DD>.md`) and the harpa repo path detected in Step 1, if any
- one line in the decision log: the gate and the scaffold were not executed, the Framer path
  does not use them

## Step 4: call phase-5-handoff.md in "framer terminal" mode

Phase 5 already has conditional variants (production override, custom brand, etc.). Add/use the "framer terminal" variant there. It prints the instructions for how to enter the `harpa-lpbuilder` session and the path of the generated handoff file.

## One sharp caution

Never scaffold a prototype folder on the Framer path. The single handoff file in the cwd is the whole output: `harpa-lpbuilder/.claude/rules/` already carries the token/component equivalents (duplicating creates drift), the Develop phase is not used (the Claude session inside `harpa-lpbuilder` does that work), and the `mcp-unframer-co` MCP belongs to that session, not this one. If the builder doesn't have the MCP configured, the markdown handoff still works. Mention they can add the MCP to their Claude config (`~/.claude.json`, `mcpServers` block) for the live-canvas path next time.
