name: Validate

on:
  pull_request:
    paths:
      - "**/SKILL.md"
      - "skills/**"
      - "**/agents/**"
      - ".bb/tasks/**"
      - ".github/scripts/**"
      - ".github/workflows/validate.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Validate frontmatter
        run: bun .github/scripts/validate-frontmatter.ts

      - name: Lint task briefs
        run: |
          shopt -s nullglob
          briefs=(.bb/tasks/*/spec.md)
          if [ ${#briefs[@]} -eq 0 ]; then
            echo "Nenhum brief em .bb/tasks/ — nada pra lintar."
            exit 0
          fi
          python3 plugins/bb/skills/spec/scripts/lint_spec.py "${briefs[@]}"

      - name: Check formatting
        run: bun run fmt:check
